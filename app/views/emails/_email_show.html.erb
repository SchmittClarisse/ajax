<div class="container">
  <div class="col-md-6"id="show">
    <p>
      <h2>Object:</h2>
      <%= @email.object %>
    </p>
    <p>
      <strong>Body:</strong>
      <%= @email.body %>
    </p>
  
    <div class="container d-inline-flex p-2">
      <%= link_to "Supprimer", email_path(@email), method: :delete, class: "btn btn-danger", remote: true %>
      <%= link_to "Marqué comme Lu", edit_email_path(@email), class: "btn btn-lg-outline" %>
    </div>
  </div>
</div>
